#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <string.h>

int main(){

    // float a ;
    // scanf("%f", &a);
    // int celli_Value = ceil(a);  This functions make the float value nearest higher int
    // printf("%d", celli_Value);


    // float a ;
    // scanf("%f", &a);
    // int floor_Value = floor(a);  This functions make the float value nearest lower
    // printf("%d", floor_Value);

    // float a; 
    // scanf("%f", &a);
    // int round_Value = round(a); This functions make the float value higher if it's 0.5 or higher than 0.5 or if its less than that make nearest lower 
    // printf("%d", round_Value);

    // double a;
    // scanf("%lf", &a);
    // double sqrt_Value = sqrt(a); This functions make any value root over value 
    // printf("%lf", sqrt_Value);

    // int a , b; 
    // scanf("%d %d", &a, &b);
    // int pow_Value = pow(a , b); This functions make two int value one to the power 
    // printf("%d", pow_Value);
    
    // int a; 
    // scanf("%d", &a);
    // int abs_Value = abs(a); This functons make any negetive value into positve value 
    // printf("%d", abs_Value);

    
    return 0;
}